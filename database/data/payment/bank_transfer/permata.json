{
    "payment_type": "bank_transfer",
    "bank_transfer": {
        "bank": "permata",
        "permata": {
            "recipient_name": "${{full_name}}"
        }
    },
    "transaction_details": {
        "order_id": "${{order_id}}",
        "gross_amount": "${{order_amount}}"
    }
}
